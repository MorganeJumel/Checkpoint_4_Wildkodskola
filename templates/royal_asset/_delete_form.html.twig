<form method="post" action="{{ path('royal_asset_delete', {'id': royal_asset.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ royal_asset.id) }}">
    <button class="btn">Delete</button>
</form>
