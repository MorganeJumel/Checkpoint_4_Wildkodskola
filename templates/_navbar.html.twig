{% block header %}
    <div class="navbar">
        <h1 class="wildkodskola-logo"><a href="/">Kingdom of Wildkodskola</a></h1>
        <ul class="menu">
            <li><a href="#">The Royal Family</a></li>
            <li><a href="{{ path('royal_asset_index') }}">Royal Assets</a></li>
            <li><a href="{{ path('booking_index') }}">Buy Tickets</a></li>
            <li><a href="#">Plan your visit</a></li>
            <li>
            {% if app.user %}
                <a href="{{ path('profile') }}">{{ app.user.firstname }} {{ app.user.lastname }}</a>
                <a href="{{ path('app_logout') }}">Log out</a>
            {% else %}
                <a href="{{ path('app_login') }}">Sign in</a>
            {% endif %}
            </li>
        </ul>
    </div>
{% endblock %}